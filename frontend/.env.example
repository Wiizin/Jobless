# Base URL of the jobless FastAPI backend.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Only needed once the backend's SESSION_SECRET is changed away from its
# insecure default ("dev-secret-change-me"). While it is the default,
# verify_session_token is a no-op and this can stay unset.
# NEXT_PUBLIC_SESSION_TOKEN=
